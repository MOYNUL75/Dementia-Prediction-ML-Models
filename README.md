# Dementia-Prediction-ML-Models
This project uses machine learning to predict dementia based on clinical and demographic data. It includes data preprocessing, feature selection, and classification models (e.g., Logistic Regression, SVM, Random Forest) to support early detection and intervention.
